#Hash-table =
# defined as a data structure used to insert, look up, 
# and remove key-value pairs quickly. It operates on the hashing concept, 
# where each key is translated by a hash function into a distinct index in an array. 